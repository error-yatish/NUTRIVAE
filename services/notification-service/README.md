# notification-service

Planned asynchronous consumer for in-app and email notifications. Publish domain events such as `leave.requested`, `leave.approved`, and `candidate.stage_changed`; keep delivery failures out of request transactions.
